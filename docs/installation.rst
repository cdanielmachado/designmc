.. highlight:: shell

============
Installation
============


Stable release
--------------

To install designmc, run this command in your terminal:

.. code-block:: console

    $ pip install designmc

This is the preferred method to install designmc, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for designmc can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/cdanielmachado/designmc

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/cdanielmachado/designmc/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/cdanielmachado/designmc
.. _tarball: https://github.com/cdanielmachado/designmc/tarball/master
